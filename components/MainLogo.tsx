import React from "react";
const MainLogo = ({ className }: any) => {
  return (
    <>
      <img
        className={`${className}`}
        src="images/footerLogo.png"
        alt=""
      />
    </>
  );
};

export default MainLogo;
